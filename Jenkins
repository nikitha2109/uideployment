pipeline {
    agent any

    

    stages {
        stage('Build') {
            steps {
                echo 'Build'
                 sh 'npm install'
            }
        }

        stage('Push') {
            steps {
                echo 'Push'

              sh 'aws s3 cp dist/app.js s3://uideploy/'
            }
        }
}
}
